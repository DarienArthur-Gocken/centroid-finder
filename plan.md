Start with VideoProcessor.java
    - process the video
    - send to ImageSummaryApp frame by frame 

ImageSummaryApp.java
    - it will need to be changed for taking in a frame or picture as a method 
    - need to separate the arguments away from the logic
    
Both needs main methods to handle the inputs 

